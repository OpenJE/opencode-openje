{
  "name": "opencode-openje",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir=dist --target=bun --external @opencode-ai/plugin",
    "check": "tsc --noEmit",
    "test": "bun test",
    "prepare": "bun run build"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "bun-types": "latest",
    "typescript": "^5.8.3",
    "zod": "^3.22.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.14.48"
  },
  "peerDependenciesMeta": {
    "@opencode-ai/plugin": {
      "optional": true
    }
  },
  "opencode": {
    "type": "plugin",
    "hooks": ["tool"]
  }
}